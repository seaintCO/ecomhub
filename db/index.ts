// Ecom Hub uses Supabase for authentication and paid-access records.
// The D1 example routes are retained as inactive reference material only.
export function getDb(): any {
  throw new Error("This starter's D1 example is not enabled for Ecom Hub.");
}
